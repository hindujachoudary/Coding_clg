int low=0,high=n-1;
while(low<=high){
	int low=0,high=n-1;
	while(low<=high){
		int mid = (low+high)/2;
		if(a[mid]==x){
			cout<<mid+1<<endl;
			break;
		}
		else if(a[mid]> x)
			high=mid-1;
		else
			low=mid+1;
	}
}